
// 找到名字为Inateck Origin的进程，并中止进程
pub fn kill_process(proc_name:&str) {
    // let mut sys = sysinfo::System::new();
    // sys.refresh_processes();
    // for process in sys.processes_by_name(proc_name.as_ref()) {
    //     if process.kill() {
    //         println!("Process {} killed", proc_name);
    //     }
    // }
}